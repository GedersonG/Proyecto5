<%- include('./partials/_header'); %> 

<div class="container">
    <div class="row mt-4">
        <div class="col-md">
            <table class="table table-bordered table-hover">
                <thead>
                    <th>ID</th>
                    <th>Cargo</th>
                    <th>Nombre</th>
                    <th>Contraseña</th>
                    <th>Telefono</th>
                    <th>Direccion</th>
                    <th>Accion</th>
                </thead>
                <tbody>
                    <% if(data) { %>
                        <% for (var i=0; i< data.length ; ++i) {%>
                            <tr>
                                <td><%= (i + 1) %> </td>
                                <td><%= data[i].id_Cargo %> </td>
                                <td><%= data[i].nombre %> </td>
                                <td><%= data[i].clave %> </td>
                                <td><%= data[i].telefono %> </td>
                                <td><%= data[i].direccion %> </td>
                                <td>
                                    <a href="/delete/<%= data[i].id_Usuario %>" class="btn btn-danger">Eliminar</a>
                                    <a href="/update/<%= data[i].id_Usuario %>" class="btn btn-info mt-1">Editar</a>
                                </td>
                            </tr>
                        <% } %>
                    <% } %> 
                </tbody>
            </table>
        </div>
        <br>
        <div class="col-md-5">
            <div class="card">
                <div class="card-body">
                    <form action="/add" method="post">
                        <div class="form-group mt-1">
                            <input type="text" name="id_Cargo" placeholder="Cargo" class="form-control" value="">
                        </div>
                        <div class="form-group mt-1">
                            <input type="text" name="clave" placeholder="Contraseña" class="form-control" value="">
                        </div>
                        <div class="form-group">
                            <input type="text" name="nombre" placeholder="Nombre" class="form-control" value="">
                        </div>
                        <div class="form-group mt-1">
                            <input type="text" name="telefono" placeholder="Telefono" class="form-control" value="">
                        </div>
                        <div class="form-group mt-1">
                            <input type="text" name="direccion" placeholder="Dirección" class="form-control" value="">
                        </div>
                        <button type="submit" class="btn btn-primary mt-1">
                            Añadir nuevo usuario
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('./partials/_footer'); %> 